class parm:
    global  H,dict_users,flag
    global net_glob, num_workers, count_number, client_net, client_model_version, client_intensity
    global t_train, saved_result,sum_t
    loss_locals = list()
    comm_loss = list()
    comm_list = list()